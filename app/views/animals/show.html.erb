<div class="container">
    <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active container-fluid">
                <%= cl_image_tag @animal.photo, crop: :fit%>
            </div>
            <div class="carousel-item">
                <img src="https://images.unsplash.com/photo-1462218503178-e822ff881c43?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1950&q=80" class="d-block w-100" alt="...">
            </div>
            <div class="carousel-item">
                <img src="https://grandbaby-cakes.com/wp-content/uploads/2015/06/Balsamic-Brown-Sugar-Lamb-Chops-1.jpg" class="d-block w-100" alt="...">
            </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
    <div class="description rows d-flex mt-4">
        <div class="colums col-8">
            <h1>
                <%= @animal.name %>
            </h1>
            <br>
            <h3>
                <%= @animal.race %>
            </h3>
            <br>
            <%= @animal.description %>
        </div>
        <div>
          <h3 class="text-secondary">Adresse :</h3>
          <div
            id="map"
            style="width: 400px;
            height: 300px;"
            data-markers="<%= @markers.to_json %>"
            data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
          </div>
        </div>
    </div>
</div>
<div class="container">
        <div class="form-wrapper ">
            <h3 class="text-secondary">Choisir les dates :</h3>
            <%= simple_form_for [@animal, @reservation] do |t| %>
            <%= t.input :start_date, as: :string, input_html: {id: "start_date"}, label: "Du" %> <br>
            <%= t.input :end_date, as: :string, input_html: {id: "end_date"}, label: "Au" %> <br>
            <%= t.submit "Bêeee", class: "btn btn-rent mr-5" %>
              <% if current_user == @animal.user %>
              <%= link_to "Supprimer", animal_path(@animal), class: "btn btn-danger mr-5",
                                method: :delete,
                                data: { confirm: "Voulez-vous vraiment tuer?" } %>
              <% end %>
              <%= link_to "Retour", animals_path, class: "btn btn-back " %>
            <% end %>
        </div>
    </div>
